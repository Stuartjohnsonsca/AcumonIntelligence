# Acumon Intelligence Website — Project Context

## Project Summary
Full-stack web portal for Acumon Intelligence at www.acumonintelligence.com
Provides authentication, subscription management, and routing to AI product tools.

## Repository
https://github.com/Stuartjohnsonsca/AcumonIntelligence.git

## Project Location
c:\Users\stuart\OneDrive - Johnsons Financial Management\AI Toolkit - Documents\Tools\Website\acumon-website\

## Tech Stack
- Next.js 16.1.6 (App Router)
- TypeScript
- Tailwind CSS + shadcn/ui (custom build, components in /components/ui/)
- NextAuth.js v5 (beta.25) — Credentials provider, JWT sessions
- Prisma ORM + Supabase PostgreSQL
- Nodemailer (Office 365 SMTP, agents@acumon.com)
- Stripe (placeholder, not yet live)
- **Google Gemini API** (gemini-2.5-flash) — AI document extraction engine (paid tier, dynamic rate-limit handling)
- **Azure Blob Storage** (acumondataextractor) — temporary file staging for extraction
- **Xero OAuth2 API** — accounting system integration (read-only transactions, chart of accounts)
- ExcelJS — Excel export generation (server-side + client-side spreadsheet parsing)
- JSZip — Server-side ZIP extraction

## Hosting
- Vercel (connected to GitHub, auto-deploy on push)
- Domain: www.acumonintelligence.com (GoDaddy DNS → Vercel)
- Wildcard: *.acumonintelligence.com for product subdomains

## Database
- Supabase project: imiuiqvyaybakfhjgmpt
- URL: https://imiuiqvyaybakfhjgmpt.supabase.co
- Pooler: aws-1-eu-north-1.pooler.supabase.com:6543

## Key Files
- /lib/auth.ts — NextAuth configuration (credentials + 2FA)
- /lib/db.ts — Prisma client singleton
- /lib/email.ts — Nodemailer email functions (OTP, reset, welcome)
- /lib/permissions.ts — Role checking helpers
- /lib/products.ts — Product list + URL prefix constants
- /lib/utils.ts — cn(), formatCurrency(), formatDate(), generateOTP()
- /lib/azure-blob.ts — Azure Blob Storage helpers (upload, move, download)
- /lib/gemini-extractor.ts — Gemini AI extraction + category inference (5 retries, dynamic backoff with server-hint parsing, jitter)
- /lib/xero.ts — Xero OAuth2 helpers (authorize, token exchange/refresh, AES-256-GCM encryption, API wrappers)
- /prisma/schema.prisma — Full DB schema
- /prisma/seed.ts — Seeds initial firm, user (Stuart Thomson ST001), and 10 products
- /middleware.ts — Auth guard for /my-account and /tools routes
- /components/Navbar.tsx — Main navigation (logo, dropdowns, login/logout)
- /components/AuthProvider.tsx — NextAuth SessionProvider wrapper
- /components/my-account/ — MyAccountClient, UsersTab, ClientsTab, SubscriptionsTab
- /components/admin/ — AdminClient, ProductsAdmin, FirmsAdmin
- /components/tools/DataExtractionClient.tsx — Data Extraction tool UI (split screen)

## Page Routes
- / — Home (hero + features)
- /about — About page
- /login — Email + password login
- /login/2fa — OTP verification (6-digit code)
- /login/reset — Forgot password
- /login/reset/verify — Set new password (token from email link)
- /product-access?prefix=X — Auth + subscription check, routes internally or to subdomain
- /access-denied?prefix=X — No access page with buy option
- /subscribe — Shopping basket for subscriptions (multi-product, prices from DB, Stripe placeholder)
- /my-account — Tabbed portal (Users/Clients/Subscriptions based on role)
- /my-account/admin — Super Admin panel (Products + Firms)
- **/tools/data-extraction** — Financial Data Extraction tool (split screen, AI extraction)

## API Routes
- POST /api/auth/get-user-id — Returns user ID by email (2FA step)
- POST /api/auth/resend-2fa — Resend OTP code
- POST /api/auth/forgot-password — Send password reset email
- POST /api/auth/reset-password — Reset password with token
- GET/POST /api/users — List/create users (Firm Admin+)
- DELETE/PATCH /api/users/[id] — Delete/update user
- GET/POST /api/clients — List/create clients
- DELETE/PATCH /api/clients/[id] — Delete/update client
- GET/POST /api/subscriptions — List/create subscriptions
- GET /api/products — List all products
- PATCH /api/products/[id] — Update product (Super Admin only)
- POST /api/products/[id]/price-change — Schedule price change
- GET/POST /api/firms — List/create firms (Super Admin only)
- DELETE/GET /api/firms/[id] — Delete/get firm details
- POST /api/extraction/upload — Upload files (PDF/images/ZIP) for extraction job
- POST /api/extraction/process — Fire-and-forget orchestrator: splits files into adaptive batches, fires parallel batch workers, returns immediately
- GET /api/extraction/process?jobId=X — Retrieve full job results (records + files)
- POST /api/extraction/process-batch — Internal batch worker: processes a list of file IDs concurrently via Promise.allSettled (secured by NEXTAUTH_SECRET)
- GET /api/extraction/status?jobId=X — Lightweight progress endpoint: returns extracted/failed/total counts for client-side polling
- GET /api/extraction/export?jobId=X — Download Excel file with 3-tab report
- GET /api/accounting/xero/connect?clientId=X — Initiates Xero OAuth2 flow (redirects to Xero login)
- GET /api/accounting/xero/callback — OAuth2 callback: exchanges code for tokens, stores encrypted in DB, redirects to data extraction page
- GET /api/accounting/xero/status?clientId=X — Checks if client has a valid (non-expired) Xero connection
- GET /api/accounting/xero/data?clientId=X&type=accounts|transactions — Fetches chart of accounts or filtered transactions from Xero

## Auth Flow
1. User enters email + password on /login, or clicks "Sign in with Microsoft"
2. Credentials: 6-digit OTP sent to registered email → /login/2fa → twoFactorVerified=true
3. Microsoft Entra ID: multi-tenant (common endpoint) — any Microsoft work/school account can sign in
   - If user exists in DB: full session with roles (2FA auto-verified)
   - If user not in DB: msalPendingSetup=true, needs Super Admin to assign firm
4. Password min length: 8 characters

## Role Hierarchy (highest to lowest)
1. Super Admin (isSuperAdmin=true) — system-wide, all rights + admin panel
2. Firm Admin (isFirmAdmin=true) — manage users + subscriptions for firm
3. Portfolio Owner (isPortfolioOwner=true) — manage clients, assign subscriptions
4. User — access tools for assigned clients with active subscriptions

## Products (10 total)
Statutory Audit: DateExtraction, DocSummary, PortfolioExtraction, FSChecker, Sampling
Internal Audit (Assurance menu): Governance, CyberResiliance, TalentRisk, ESGSustainability, Diversity

Product subdomain pattern: {urlPrefix.toLowerCase()}.acumonintelligence.com

## Initial Users
- Stuart Thomson | ST001 | Johnsons Financial Management
- Email: stuart.thomson@johnsonsfinancial.co.uk
- Roles: Super Admin + Firm Admin + Portfolio Owner
- 2FA method: email

## Email Config
- From: agents@acumon.com
- SMTP: smtp.office365.com:587 (STARTTLS)
- Requires SMTP AUTH enabled on the mailbox in M365 Admin Centre

## Environment Variables Required
DATABASE_URL, DIRECT_URL, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
NEXTAUTH_SECRET, NEXTAUTH_URL
EMAIL_FROM, SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD
STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
GEMINI_API_KEY — Google AI API key for document extraction (project: DataExtractor, gen-lang-client-0724242070, key: DataExtractorKey)
AZURE_STORAGE_CONNECTION_STRING — Azure Blob Storage connection string (acumondataextractor)
AZURE_STORAGE_CONTAINER_INBOX=upload-inbox
AZURE_STORAGE_CONTAINER_PROCESSING=processing
AZURE_STORAGE_CONTAINER_PROCESSED=processed
DISABLE_2FA=true — Bypass 2FA for development (remove for production)
XERO_CLIENT_ID — Xero OAuth2 app client ID (from developer.xero.com)
XERO_CLIENT_SECRET — Xero OAuth2 app client secret
XERO_REDIRECT_URI — OAuth callback URL (default: {NEXTAUTH_URL}/api/accounting/xero/callback)
TOKEN_ENCRYPTION_KEY — 64-char hex string (32 bytes) for AES-256-GCM encryption of stored Xero tokens
GEMINI_MAX_CONCURRENT — Max concurrent Gemini API calls per batch worker (default: 3)

## Setup Commands (run in /acumon-website/)
npm install
npx prisma db push
npm run db:seed
npm run dev

## Deployment Notes
- vercel.json added to root — explicitly sets framework: nextjs, buildCommand includes prisma generate
- All pages using useSearchParams() wrapped in Suspense (Next.js 15 requirement)
- Nodemailer types declared in types/nodemailer.d.ts
- Build verified locally: `npm run build` passes with 0 errors, 23 pages generated
- GitHub repo: all app files are at root of repo (not in a subfolder) — Vercel Root Directory should be left empty (default)

## Auth Architecture — Page-Level Checks Only (No Proxy Protection)
- proxy.ts exists but has an empty matcher — it does NOT protect any routes
- ALL auth checks happen server-side in page.tsx files using auth() + redirect('/login')
- This is required because edge proxy/middleware cannot read session cookies during cross-site OAuth redirect chains (Microsoft Entra ID → callback → protected page)
- DO NOT add routes back to proxy.ts matcher — it breaks MSAL login flow
- Default NextAuth cookie config is used (do NOT override with custom cookie names/SameSite)
- Protected pages: /my-account, /my-account/admin, /tools/data-extraction all check auth() themselves
- Microsoft Entra ID uses multi-tenant 'common' endpoints (NOT tenant-specific) so external users can sign in
- Azure AD app registration must be set to "Accounts in any organizational directory (Multitenant)" in the Azure Portal

## Shopping Basket (Subscribe Page)
- /subscribe fetches product list + prices from GET /api/products (DB-driven, not hardcoded)
- Multi-product basket: users can add multiple products with different quantities (1, 5, 10, 20)
- Basket sidebar shows all items, per-item subtotals, and a grand total
- Checkout currently shows an order confirmation placeholder (Stripe integration to be added later)
- Pre-fill supported via ?prefix=X query parameter (e.g. from access-denied page)

## DB Tables (Data Extraction)
- extraction_jobs — one per extraction session (links client + user); includes total_files, processed_count, failed_count counters for live progress
- extraction_files — individual files in a job (tracks blob path, zip status, extraction status, file_hash, duplicate_of_id)
- extracted_records — one per extracted document (all financial fields as columns)
- account_category_learning — per-client AI learning for description→category mapping
- accounting_connections — stores encrypted Xero OAuth2 tokens per client (unique on [clientId, system]); auto-refreshes; 30-day expiry

## Parallel Extraction Pipeline Architecture
- Files are uploaded via POST /api/extraction/upload (stores in Azure inbox, creates ExtractionFile rows)
  - SHA-256 hash computed per file; duplicates detected within the same job and marked as status='duplicate' (not uploaded to blob)
  - totalFiles on ExtractionJob counts only unique files; duplicateCount tracks skipped duplicates
- POST /api/extraction/process acts as a fire-and-forget orchestrator:
  1. Calculates adaptive batch size: ≤5 files → all at once; ≤20 → 5/batch; ≤100 → 10/batch; ≤300 → 15/batch; >300 → 25/batch
  2. Fires N parallel fetch() calls to POST /api/extraction/process-batch (does NOT await them)
  3. Returns immediately with {jobId, status: "processing", totalFiles, batchCount, batchSize}
- Each batch worker (process-batch) runs in its own serverless function:
  - Processes files with concurrency limit of 2 per batch (semaphore pattern, not all-at-once)
  - Moves blobs: inbox → processing → processed (with copy-completion await before delete)
  - Calls Gemini AI for extraction, saves ExtractedRecord + category learning
  - Atomically increments processedCount/failedCount on ExtractionJob
  - When final batch completes, marks job as "complete"
- Gemini API calls have dynamic retry with exponential backoff (5 attempts, server-hint-aware delays)
  - Parses 'retry in Xs' from Gemini 429 response body and uses that as the delay (min 2s, max 60s)
  - Falls back to exponential backoff (2s, 4s, 8s, 16s) when no server hint is present
  - Adds 0-25% random jitter to prevent thundering herd across concurrent batches
  - Transient errors (429 rate limit, 500/503, timeouts) are retried; non-transient errors fail immediately
  - Error messages are stored on ExtractionFile.errorMessage for diagnostics
- Batch workers use adaptive inter-request delay: starts at 200ms, doubles on 429 errors, decays on success (min 100ms, max 15s)
- Batch orchestrator staggers batch launches by 500ms to spread initial load
- Azure blob moves await copy completion before deleting source (prevents race conditions)
- Client-side polls GET /api/extraction/status?jobId=X every 1.5s
  - Returns {total, extracted, failed, duplicated, complete} from the ExtractionJob counters
  - UI shows an animated progress bar with green (success), red (failed) segments, and duplicate count
  - Failed files show their error messages in the Extraction Details tab for diagnostics
  - When complete=true, polling stops and full results are fetched
- Navigation-safe: once orchestrator fires batches, processing continues server-side regardless of whether user stays on the page
- File status categories: uploaded, processing, extracted, failed, duplicate

## Data Extraction — Left Panel Features
- **Upload Spreadsheet**: Opens file picker for a single .xlsx or .csv file; parsed client-side (ExcelJS for xlsx, native for CSV) and displayed in an editable spreadsheet grid on the left panel
- **Load Blank Spreadsheet**: Creates an empty editable grid with standard accounting columns (Date, Reference, Contact, Description, Account Code, Net, Tax, Gross); supports clipboard paste for bulk data entry; user can add more rows
- **Collate from Xero**: Checks if client has a valid Xero connection via GET /api/accounting/xero/status
  - If not connected: redirects to GET /api/accounting/xero/connect to initiate OAuth2 flow
  - If connected: shows modal with chart of accounts multi-select + date range picker; fetches transactions and populates left panel grid
  - Xero OAuth2 flow: redirect → Xero login → callback → token exchange → encrypted storage (AES-256-GCM) in accounting_connections table
  - Tokens auto-refresh on expiry; connection expires after 30 days
  - Scopes: offline_access, openid, profile, email, accounting.transactions.read, accounting.settings.read, accounting.contacts.read

## Xero Integration Setup (User Actions Required)
1. Register app at developer.xero.com
2. Set redirect URI: https://acumon-intelligence.vercel.app/api/accounting/xero/callback
3. Add XERO_CLIENT_ID, XERO_CLIENT_SECRET to Vercel env vars
4. Generate TOKEN_ENCRYPTION_KEY: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
5. Add TOKEN_ENCRYPTION_KEY to Vercel env vars
6. Run prisma db push after schema update (adds accounting_connections table)

## Outstanding Tasks
- Add GEMINI_API_KEY, AZURE_STORAGE_CONNECTION_STRING, AZURE_STORAGE_CONTAINER_* to Vercel env vars
- Set SMTP_PASSWORD in Vercel env vars once Exchange password is known
- Enable SMTP AUTH on agents@acumon.com in Microsoft 365 Admin Centre
- Configure Stripe when ready for live payments
- Complete Xero developer app registration (user has API details pending)
- Upgrade Gemini to paid plan and update GEMINI_API_KEY
- Implement "Send by Email" feature on extraction results
- Add remaining tool pages (DocSummary, FSChecker etc.) at /tools/*
